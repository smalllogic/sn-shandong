### Sinower-Shandong 厨房设备外贸电商系统安全白皮书

本报告详尽描述了 Sinower-Shandong 系统的安全架构设计、防御策略实现及其技术选型背后的商业逻辑。在日益复杂的网络环境下，该系统通过多层次的防御体系，确保了外贸业务的连续性和企业核心资产的安全性。

---

#### 1. 安全设计理念：深度防御 (Defense in Depth)

Sinower-Shandong 的安全架构遵循“最小权限原则”和“防御深度”理念。安全不仅是代码层面的补丁，更是贯穿于认证、授权、传输、存储及审计的闭环体系。

**核心目标：**
*   **防止未授权访问**：严格区分管理员与访客权限。
*   **对抗恶意自动化攻击**：识别并封禁扫描器、爆破工具。
*   **完整的操作留痕**：确保任何敏感操作均可被追溯至个人。
*   **数据隐私保护**：防止敏感配置和用户隐私在日志中泄露。

---

#### 2. 身份认证与会话管理 (Authentication & Session)

系统采用业界标准的 **Devise** 框架进行身份验证，并针对高安全性需求进行了二次定制。

**2.1 强化认证机制**
*   **密码策略**：基于 `bcrypt` 的高强度哈希加密，有效对抗彩虹表攻击。
*   **账户锁定 (Lockable)**：在 `User` 模型中集成了 `:lockable` 模块。当检测到连续 5 次登录失败时，账户将自动锁定 1 小时。这一机制极大地提高了暴力破解的成本。
*   **Warden 钩子审计**：通过 `config/initializers/warden_hooks.rb`，系统在每次成功认证后自动触发 `after_set_user` 逻辑，记录登录时间、IP 地址及 User Agent。

**2.2 登录审计与异常监控**
*   **LoginLog 追踪**：系统会自动为每次成功登录生成 `LoginLog` 记录。
*   **地理位置解析**：集成 `GeocodeLoginLogJob` 异步任务。通过外部 API 将 IP 地址解析为地理位置（省份、城市、ISP），便于安全团队识别跨国/跨地区的异常登录行为。
*   **设备指纹识别**：通过解析 `User_Agent`，系统能识别登录设备类型（iOS, Android, Windows, Mac），帮助管理员识别盗号风险。

---

#### 3. 细粒度权限控制 (Authorization - RBAC)

权限控制不仅是保护后台入口，更是对业务逻辑的精确隔离。

**3.1 基于角色的访问控制 (RBAC)**
系统在 `User` 模型中定义了四种角色级别：
1.  **Super Admin (大管理员)**：拥有最高权限，可管理系统配置、删除敏感日志、提升其他用户权限。
2.  **Admin (管理员)**：可进行产品、分类、文章的常规维护，但无权访问核心系统配置。
3.  **Editor (编辑)**：仅限内容管理权限。
4.  **User (普通用户)**：仅前台访问权限。

**3.2 控制器级安全拦截**
`Admin::BaseController` 充当了所有后台操作的安全守门员：
*   `before_action :authenticate_user!`：确保必须已登录。
*   `before_action :check_admin`：通过模型定义的 `admin_role?` 方法，拒绝非管理角色的任何访问请求。
*   **超级管理员拦截**：对于 `/admin/users` 等高敏感接口，使用 `require_super_admin` 进行二次拦截。

---

#### 4. 全方位攻击防御矩阵 (Rack-Attack Implementation)

系统引入了 **Rack-Attack** 中间件，构建了从 IP 层到业务层的立体防御网。

**4.1 Fail2Ban 扫描拦截**
针对外贸网站常遇到的恶意嗅探，系统配置了详尽的封禁路径列表：
*   **拦截范围**：涵盖 `.env`, `.git`, `wp-admin`, `.php`, `xmlrpc.php` 等典型扫描目标。
*   **惩罚机制**：1 分钟内访问 3 次此类非法路径，IP 将被封禁 24 小时。这能有效过滤掉 90% 以上的自动化攻击脚本。

**4.2 业务级速率限制 (Throttling)**
*   **登录防护**：同一 IP 每分钟限 5 次登录；同一邮箱每小时限 3 次尝试。
*   **密码重置保护**：限制每小时 3 次重置请求，防止攻击者利用邮件系统对特定用户进行骚扰或尝试劫持。
*   **防表单轰炸**：联系表单限制每 IP 每小时 10 次提交，防止垃圾询盘塞满管理员邮箱。
*   **全站防御**：针对普通页面，设置了每分钟 120 次的频率上限，过滤高频率爬虫。

**4.3 智能导出保护**
CSV 导出是耗资源操作。系统专门针对 `/admin/skus/export` 等接口设置了每小时 5 次的频率控制，防止恶意用户通过高并发请求导致服务器 OOM 或数据库阻塞。

---

#### 5. 安全审计与可追溯性 (Auditing System)

审计是系统安全的最后一道防线，也是事件响应的核心工具。

**5.1 自动化操作日志 (OperationLog)**
系统在 `Admin::BaseController` 中利用 `after_action` 实现了“全自动留痕”：
*   **监控动作**：仅记录 POST, PATCH, PUT, DELETE 等会引起数据变动的动作。
*   **记录维度**：操作人 ID、控制器动作（如 `SkusController#update`）、请求路径、原始 IP 地址、HTTP 状态码。
*   **详细参数记录**：以 JSON 格式存储请求参数，方便发生争议或误操作时进行数据比对。

**5.2 敏感数据脱敏 (Data Masking)**
在记录操作日志前，系统通过 `filter_sensitive_params` 进行深度清理：
*   **过滤关键字**：`password`, `token`, `secret`, `key` 等敏感字段会被统一替换为 `[FILTERED]`。
*   **递归处理**：支持嵌套 Hash 和数组的递归过滤，确保即使在复杂的表单提交中，敏感信息也不会进入数据库日志。

---

#### 6. Web 安全加固 (Standard Web Security)

基于 Ruby on Rails 8 的原生优势，系统默认开启了多种现代安全机制：
*   **CSRF 防护**：所有非 GET 请求必须携带 `authenticity_token`。
*   **XSS 防护**：ERB 模板默认对所有变量进行 HTML 转义。
*   **SQL 注入防护**：全面采用 Active Record 的参数化查询。
*   **日志过滤**：在 `config/initializers/filter_parameter_logging.rb` 中配置了全局参数过滤，确保敏感信息不会出现在应用日志（如 Railway 控制台）中。

---

#### 7. 技术取舍与未来安全演进

**7.1 当前取舍**
*   **自研日志 vs. 外部 Gem**：没有使用 `paper_trail` 等版本控制 Gem，而是自研了轻量级的 `OperationLog`。
    *   **理由**：`paper_trail` 会大幅增加数据库体积。外贸系统更关注“谁在什么时候做了什么”，而非数据的每一个细微版本，自研方案更轻量、查询效率更高。
*   **本地速率限制**：Rack-Attack 使用 `Rails.cache` (Solid Cache)。
    *   **改进空间**：在超高并发下，可能需要迁移至 Redis 以获得更好的性能稳定性。

**7.2 未来规划**
1.  **双因子认证 (2FA)**：针对 Super Admin 角色，未来计划引入 TOTP (Google Authenticator) 认证，进一步提升管理后台安全性。
2.  **内容安全策略 (CSP)**：逐步启用 `content_security_policy.rb`，通过严格的源控制，彻底杜绝内联脚本攻击和非法资源加载。
3.  **WAF 联动**：将 Rack-Attack 的封禁逻辑与 Cloudflare WAF 联动，实现在边缘节点直接阻断攻击源。

---

### 结语

Sinower-Shandong 的安全体系不是静态的，而是一个动态演进的过程。从防范简单的暴力破解到复杂的业务逻辑审计，系统构建了一道坚固的“安全长城”，为跨境电商的稳健运行保驾护航。


---

#### 8. 访客流量监控与行为分析 (Traffic Monitoring)

除了对管理员的审计，系统还建立了全面的访客监控机制，这在防御高级持续性威胁 (APT) 和异常流量攻击中发挥着重要作用。

**8.1 全量访问记录 (VisitRecord)**
*   **捕获维度**：系统记录了所有访客的 `session_id`、IP 地址、`user_agent` 和访问时间。
*   **地理位置画像**：与登录日志类似，`VisitRecord` 集成了 `GeocodeVisitRecordJob`。通过对海量访客 IP 的地理位置分析，管理员可以识别出非目标市场的异常突发流量。
*   **设备合规性**：通过 `device_info` 识别访客设备，有助于识别使用模拟器或特定爬虫工具的恶意请求。

**8.2 数据保留与清理策略**
*   **强制合规**：在 `Admin::VisitRecordsController` 中，系统强制规定只能清理 7 天以前的记录。这一约束确保了在发生安全事件时，技术团队至少拥有最近一周的原始流量数据进行回溯分析。
*   **自动清理机制**：通过 `cleanup_old_records` 方法，系统能够定期释放数据库空间，防止审计数据撑爆磁盘导致的拒绝服务 (DoS)。

---

#### 9. 基础设施与存储安全 (Infrastructure & Cloud)

**9.1 Cloudflare R2 安全集成**
*   **S3 协议兼容**：系统使用 `aws-sdk-s3` 协议与 Cloudflare R2 通信。通过使用 IAM 级别的 Access Key 和 Secret Key，并限制其仅拥有 R2 存储桶的读写权限，实现了最小权限控制。
*   **私有存储策略**：产品图片和资源虽然通过 CDN 分发，但其原始存储地址是隐藏的，防止了绕过 CDN 直接攻击存储源站的风险。

**9.2 邮件服务安全 (Resend)**
*   **API 密钥管理**：使用 Resend 现代化的邮件服务，避免了配置传统 SMTP 服务器带来的泄露风险。
*   **域名认证**：通过 SPF, DKIM 和 DMARC 认证，确保系统发出的询盘邮件和重置密码邮件不会被拦截，同时也防止了他人冒充系统发送钓鱼邮件。

---

#### 10. 应急响应与灾难恢复

**10.1 实时日志流**
得益于 Railway 的基础设施，系统的标准输出日志被实时捕获并支持检索。安全管理员可以快速通过关键词过滤异常报错，发现潜在的注入尝试或配置错误。

**10.2 数据库快照**
生产环境采用 PostgreSQL。系统配置了自动备份与点对点恢复 (PITR) 能力。一旦发生数据篡改或勒索攻击，系统可以在数分钟内恢复到攻击发生前的任意时间点。

---

#### 11. 总结

Sinower-Shandong 的安全体系是一个**立体化、可审计、自防御**的现代 Web 应用典范。通过 Rails 8 的原生优势与 Rack-Attack、Devise 等组件的深度定制，系统在保证性能的同时，构建了极高的安全水位线。

未来的安全工作将重点围绕“主动防御”展开，引入更多 AI 驱动的流量识别算法，进一步降低安全运维的成本，提升外贸业务的全球竞争力。
